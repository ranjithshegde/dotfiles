[global]
frame_width = 2
frame_height = 2
font = Monospace 12
icon_theme = Adwaita
enable_recursive_icon_lookup = true
sort = true

[general]
background = "#2B2B2B"
foreground = "#AAAAAA"

[icon]
# Note: dunst does not have a direct setting for icon_color, so this line is not valid in dunst.
